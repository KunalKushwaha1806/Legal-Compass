# ─── Legal Compass — Requirements ───────────────────────────────
# Core (Flask server + NLP retrieval)
flask>=2.3.0
flask-cors>=4.0.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
requests>=2.31.0
tqdm>=4.65.0

# ─── ML / Model Training (needed for data/model pipeline) ────────
# Install only if you plan to fine-tune or run the Flan-T5 model
transformers>=4.35.0
torch>=2.0.0
sentencepiece>=0.1.99
