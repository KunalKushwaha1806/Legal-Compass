# Frontend connects to the Node backend at localhost:3001 via Vite proxy.
# No secret keys needed in the frontend .env — all auth goes through the backend.

# Only needed if you're NOT using the Vite proxy (i.e., deploying separately).
VITE_API_URL=http://localhost:3001/api
